.password-reset
  %h2 Change your password
  %p Your password has been reset. Choose a new password below.
  = form_for [@user, @user_token] do |form|
    .password-field
      = form.label :password
      = form.password_field :password
    .submit-field
      = form.submit 'Update Password'
